source 'https://rubygems.org'

gem 'html-proofer'
gem "github-pages", group: :jekyll_plugins
gem 'tzinfo-data'
gem 'webrick'

# gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw] #use this if you are running on windows
# gem "webrick", "~> 1.8" #use this if you are running on windows

group :jekyll_plugins do
  gem 'jekyll-archives'
end

